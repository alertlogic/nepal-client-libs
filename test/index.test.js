/* eslint-disable no-undef */
/* eslint-disable no-new-require */
/* eslint-disable no-unused-vars */
const should = require('chai').should();
const AIMSClient = require('../index.js');

const test = 1;

describe('#stuff', () => {
  it('passes testing', () => {
    test.should.equal(1);
  });
});
